EM.group <- function(pars, constrain, PrepList, list, debug)
{
    if(debug == 'EM.group') browser()
    
    
#     ret <- list(pars=pars, cycles = cycles - BURNIN - SEMCYCLES, info=as.matrix(info), 
#                 longpars=longpars, converge=converge)
#     ret
}

